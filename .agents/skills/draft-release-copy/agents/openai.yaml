interface:
  display_name: "Draft Release Copy"
  short_description: "Draft reusable release-copy updates from release evidence and publish metadata"
  default_prompt: >-
    Use $draft-release-copy to collect release evidence, prepare publish
    configuration and README updates, and validate a reusable release-copy
    plan before applying it.
