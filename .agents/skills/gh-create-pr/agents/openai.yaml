interface:
  display_name: "GH Create PR"
  short_description: "Create a guarded GitHub pull request from an already-pushed branch"
  default_prompt: >-
    Use $gh-create-pr to collect repo and PR-template context, draft a
    guarded title and body, validate the normalized create request, and open
    the pull request only after validation passes.
