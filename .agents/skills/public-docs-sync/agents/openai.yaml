interface:
  display_name: "Public Docs Sync"
  short_description: "Audit and synchronize public repository docs against runtime metadata and GitHub evidence"
  default_prompt: >-
    Use $public-docs-sync to audit tracked public docs against runtime
    metadata and GitHub evidence, prepare deterministic sync edits, and
    apply them only after validation.
